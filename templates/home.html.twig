{% extends "base.html.twig" %}

{% block title %}
    Accueil
{% endblock %}

{% block body %}
        <h1>Nos produit phares !</h1>
        <div class="row">
            {% for product in products %}
                <div class="col">
                    <div class="card">
                        <img src="{{ product.mainPicture }}" class="img-fluid" alt="Image du produit">
                        <div class="card-body">
                            <h4 class="card-title">{{ product.name }} ({{ product.price/100 }} &euro;) </h4>
                            <span class="badge badge-info" >
                                {{ product.category.name }}
                            </span>
                            <p class="card-text">{{ product.shortDescription }}</p>
                            <a href="#" class="btn btn-primary btn-sm">Détails</a>
                            <a href="#" class="btn btn-success btn-sm">Ajouter</a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
        <hr>
        <h2>Un service irreprochable</h2>
        <P>zoerfr efeg er ge rg erg ergergerge ergergerge gergerger</P>
        <div class="row">
            {% include "shared/_assurance.html.twig" with {
                'color': 'primary',
                'icone': 'fas fa-truck',
                'text': 'sljbdf sd sdfsdf sdf sdf sd f sd fs df je meiux reprendre le travaila la sortie de l0école'
            } %}

            {% include "shared/_assurance.html.twig" with {
                'color': 'success',
                'icone': 'fas fa-check',
                'text': 'sljbdf sd sdfsdf sdf sdf sd f sd fs df je meiux reprendre le travaila la sortie de l0école'
            } %}

            {% include "shared/_assurance.html.twig" with {
                'color': 'warning',
                'icone': 'fas fa-lock',
                'text': 'sljbdf sd sdfsdf sdf sdf sd f sd fs df je meiux reprendre le travaila la sortie de l0école'
            } %}

            <div class="col">
                <div class="card bg-primary text-white text-center">
                    <div class="card-body">
                        <i class="fas fa-truck fa-2x mb-3"></i>
                        <p class="card-text">je ne suis pas la pour le moment, rééseller encore une fois, rééseller
                            encore une fois
                        </p>
                    </div>
                </div>
            </div>
        </div>
{% endblock %}