<nav class="navbar navbar-light bg-light navbar-expand-md">
    <div class="container">
        <div class="navbar-brand">
            NADIR-SHOP
        </div>
        <div class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#monMenuDeroulant">
            <span class="navbar-toggler-icon"></span>
        </div>

        <div class="collapse navbar-collapse" id="monMenuDeroulant">

            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path("homepage") }}">Home
                    </a>
                </li>
                {% for categorie in categoryRepository.findAll() %}
                    <li class="nav-item">
                        <a class="nav-link"
                           href="{{ path("product_category", {'slug': categorie.slug}) }}">{{ categorie.name }}</a>
                    </li>
                {% endfor %}
            </ul>
            <ul class="navbar-nav">
                <li class="nav-item"><a href="{{ path('contact') }}" class="nav-link" >Contactez-nous</a></li>
                <li class="nav-item">
                    <a href="{{ path('cart_show') }}" class="nav-link">
                        <i class="fas fa-shopping-cart"></i>
                        Panier({{ cartService.total | amount }})
                    </a>
                </li>
                {% if app.user %}
                    <li class="nav-item"><a href="{{ path('purchases_index') }}" class="nav-link">Mes commandes</a></li>
                    <li class="nav-item"><a href="{{ path('authentification_logout') }}"
                                            class="btn btn-danger">Logout</a>
                    </li>
                {% else %}
                    <li class="nav-item"><a href="{{ path('register') }}" class="nav-link">Inscription</a></li>
                    <li class="nav-item"><a href="{{ path('authentification_login') }}"
                                            class="btn btn-success">Login</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>